# Scoped to what api/predict.py actually imports at runtime. statsmodels
# is intentionally omitted: it's only imported inside
# features.prune_collinear_features, which api/predict.py never calls
# (it runs the pipeline with skip_vif=True to keep cold starts within
# Vercel's execution-time budget). Add it back here if you flip skip_vif.
numpy
pandas
scipy
scikit-learn
lightgbm
