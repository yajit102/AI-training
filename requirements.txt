# Core
numpy
pyyaml
tqdm

# FlowPredict TAT model (src/flowpredict)
pandas
lightgbm
# scipy/statsmodels are only needed for the offline VIF-pruning step
# (features.prune_collinear_features, skip_vif=False) -- not used by
# api/predict.py, which always runs with skip_vif=True.
scipy
statsmodels

# ML frameworks — uncomment what you need
# torch
# transformers
# datasets
# accelerate
# peft
# trl

# Vision
# torchvision
# opencv-python
# pillow

# Experiment tracking
# wandb
# mlflow

# Dev
pytest
ruff
